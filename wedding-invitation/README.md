# Артём & Юлия — сайт-приглашение

Одностраничный сайт-приглашение на свадьбу 19 сентября 2026 года. Чистый
HTML/CSS/JS без сборки и зависимостей — открывается как есть, кладётся на
любой статический хостинг.

Оформление опирается на данные скилла `ui-ux-pro-max` (подбор пары шрифтов,
акценты, чек-лист доступности) и на принципы типографики и вёрстки школы
бюро Горбунова: модульная сетка отступов, единая шкала заголовков, длина
строки под чтение (~62 символа), неразрывные пробелы после однобуквенных
предлогов/союзов, типографское тире, кавычки-«ёлочки».

## Структура

```
wedding-invitation/
  index.html
  css/style.css
  js/main.js
  assets/photos/   ← сюда кладутся реальные фотографии (см. ниже)
```

Разделы страницы (все — из ТЗ): герой с датой и обратным отсчётом →
фотогалерея → программа дня (церемония + банкет) → место проведения с
картой → дресс-код с палитрой → подарки → RSVP-форма → закрывающий блок.

## 1. Добавить настоящие фотографии

Файлы ищутся по путям ниже. Пока их нет — на месте фото аккуратный
градиент-заглушка с подписью, ничего не «ломается».

| Файл | Что туда положить |
|---|---|
| `assets/photos/hero.jpg` | Портретный кадр поцелуя на закате (силуэт, контровой свет) — идёт на весь экран в шапке сайта |
| `assets/photos/gallery-1.jpg` | Объятия вдвоём в поле на фоне облачного неба (широкий кадр) — крупная плитка в галерее |
| `assets/photos/gallery-2.jpg` | Кружение в платье посреди поля |
| `assets/photos/gallery-3.jpg` | Идут по полю, держась за руки |
| `assets/photos/gallery-4.jpg` | Объятия крупным планом |

Требования: JPG, ширина ≥ 1600px для `hero.jpg` (растягивается на весь
экран), ≥ 1000px для остальных. Изображения подключены через `<img>` и
CSS-фон с `object-fit: cover` — обрезка по центру, кадрировать заранее не
обязательно.

## 2. Подключить точный адрес на карте

Сейчас карта ресторана «Белый рояль» встроена по текстовому поиску
(`.map-embed iframe` в `index.html`, секция «Место проведения») — работает,
но показывает примерный район города, а не точную метку.

Чтобы вставить точный адрес:

1. Откройте [yandex.ru/map-constructor](https://yandex.ru/map-constructor/).
2. Создайте карту типа **«Одна метка»**, укажите точный адрес или координаты
   ресторана.
3. Скопируйте предложенный сервисом код `<iframe>` и замените им блок
   `.map-embed` в `index.html` (комментарий в коде отмечает это место).

Кнопки «Открыть в Яндекс Картах» рядом с картой ведут на текстовый поиск —
их тоже стоит заменить на прямые ссылки с координатами, когда адрес будет
известен точно.

## 3. Подключить приём заявок RSVP

Форма (`#rsvp`) работает уже сейчас без какой-либо настройки: она сохраняет
ответ в `localStorage` браузера гостя и, если сервер для приёма заявок не
указан, открывает у гостя почтовый клиент с уже готовым письмом на ваш
адрес — просто на «Отправить».

Чтобы получать ответы автоматически (в таблицу/почту/Telegram), откройте
`js/main.js` и заполните константы в самом начале блока RSVP:

```js
const ENDPOINT = ""; // например: "https://formspree.io/f/xxxxxxx"
const COUPLE_EMAIL = "your-email@example.com"; // ваша почта — для запасного варианта
```

Подходит любой сервис, принимающий обычный POST формы: Formspree, Getform,
Google-таблица через Apps Script Web App и т.п. Как только `ENDPOINT`
заполнен, письмо-заглушка больше не используется — ответы уходят напрямую.

## 4. Как проверить и опубликовать

Локально:

```bash
cd wedding-invitation
python3 -m http.server 8080
# открыть http://localhost:8080
```

Опубликовать можно на GitHub Pages (Settings → Pages → папка
`wedding-invitation`), Netlify/Vercel (drag-and-drop папки) или любом
статическом хостинге — сборка не нужна.

При публикации обновите в `index.html`:
- `<meta property="og:image" content="assets/photos/hero.jpg" />` — на
  полный URL (`https://ваш-домен/assets/photos/hero.jpg`), иначе превью
  ссылки в мессенджерах не подтянет картинку.

## Что уже проверено

- Адаптивная вёрстка mobile-first, брейкпоинты 375 / 390 / 412 / 768 / 1024 /
  1440px — без горизонтальной прокрутки на любой ширине.
- iOS/Android: `safe-area-inset-*` под чёлку и жестовую полосу, размер полей
  ввода ≥16px (Safari не зумит при фокусе), все интерактивные элементы
  ≥44×44px, вязкость к `100svh/100dvh` для реальной высоты экрана с учётом
  адресной строки браузера.
- Доступность: семантическая разметка, видимый фокус с клавиатуры,
  `skip-link`, подписи всех полей формы, `aria-live` на статусах формы,
  `prefers-reduced-motion` отключает анимации.
- Прогрессивное появление блоков при прокрутке проверено и на обычной
  прокрутке колесом/тачем, и на резких переходах по якорным ссылкам —
  срабатывает в обоих случаях (двойная защита: `IntersectionObserver` +
  запасная проверка позиции по скроллу).
- Форма: обязательные поля, показ поля «количество гостей» только при
  ответе «Да», состояние отправки/успеха, откат на письмо при недоступном
  сервере.

Тема сайта единая (тёплая, светлая) и намеренно не подстраивается под
тёмную тему устройства — так у всех гостей приглашение выглядит одинаково.
